[project]
name = "codlock-fitting-agent"
version = "0.1.0"
description = "CODLOCK Fitting Agent (A2A server)"
requires-python = ">=3.11"
dependencies = [
    "a2a-sdk[http-server]>=1.1.2",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "httpx>=0.27",
    "uvicorn>=0.32",
    "python-dotenv>=1.0",
    "fastapi>=0.115",
]

[project.optional-dependencies]
dev = ["pytest>=8.3", "pytest-asyncio>=0.24", "ruff>=0.7"]

[project.scripts]
fitting-agent = "codlock_agents.fitting.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/codlock_agents"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
